export type ExperienceType = {
  date: string;
  title: string;
  link: string;
  description: Array<string>;
};

export type ProjectType = {
  title: string;
  link: string;
  description: string;
  tech: Array<string>;
};
